# Tasks: compiler-backed-ci-gate

CI-only change; request the maintainer-applied `skip-changelog` label (no `compiler-version.ss` bump). Nothing here changes `compactc` behaviour — the emitter's rustfmt post-pass stays a soft-fail (2.3).

## 1. Compiler-backed CI lane

- [x] 1.1 Add a self-contained job to `.github/workflows/rust-runtime-test.yml` (name it for the lane, e.g. `compiler-backed`; `runs-on: ubuntu-latest`; set a `timeout-minutes` — 60, raised if a cold Nix cache exceeds it) that:
  (a) sets up the Nix cache via the existing `./.github/actions/setup-nix-cache` action;
  (b) installs the Rust toolchain **including `rustfmt`** — mirror the `pre-check` job's `dtolnay/rust-toolchain` + `rustup component add rustfmt` (current lines 74-78);
  (c) runs `nix build .#compactc`;
  (d) runs `cargo test -p tests-e2e-rust --locked` with **no** `--skip` flags.
  Verify: the job log shows `codegen_regression` and `rejection_corpus` executing (non-zero test counts, not filtered out) and `rustfmt --version` succeeding on the lane.
- [x] 1.2 Keep the existing `test` matrix as the non-compiler lane and document it: leave its `-- --skip rust_codegen_byte_parity --skip rust_backend_` invocation in place (those bare Ubuntu/macOS runners have no Nix and no `compactc`, so dropping the skips there would hard-fail by construction), and rewrite the workflow header comment (currently lines 12-21) to state that (i) the byte-parity and rejection gates are enforced by the new `compiler-backed` job, which has no skip path, and (ii) the matrix keeps its skips only because it is not the enforcement point. Verify: `grep -n "skip rust" .github/workflows/rust-runtime-test.yml` shows the skips only in the matrix job, and the `compiler-backed` invocation has none.
- [x] 1.3 Widen the workflow's `on.paths` so the lane triggers on compiler changes: add `compiler/**` and `flake.nix`. (The current list only matches `compiler/rust-passes*.ss` / `compiler/rust-passes/**`, so e.g. an `analysis-passes.ss` change that alters Rust emit would not run the lane.) Verify: a branch touching only `compiler/analysis-passes.ss` triggers `compiler-backed`.
- [x] 1.4 Confirm the lane fails when it should. Verified at apply time against the lane's exact command, because the CI job itself is only observable from a pushed branch: with `cargo test -p tests-e2e-rust --locked` (real `result/bin/compactc` + rustfmt) the suite is fully green; appending a line to one fixture's committed `lib.rs` makes `rust_codegen_byte_parity_against_committed_fixtures` fail (red), and restoring the file returns to green. The `compiler-backed` job runs that same command with no `--skip` path, so it goes red on the same drift. Attach the CI run link to the PR when one is opened (PR-time confirmation; not an apply-time blocker).

## 2. Gate invariants

- [x] 2.1 Add an invariant test asserting every `FIXTURES` row's contract crate is listed under `tests-e2e-rust/Cargo.toml` `[dev-dependencies]`. Put it in `tests-e2e-rust/tests/codegen_regression.rs`: parse the `FIXTURES` const and the manifest, and derive each crate name from its `dir-name`. Verify: it fails today for `compact-contract-multi-pl-call-fixture`, then passes once 2.2 lands.
- [x] 2.2 Add `compact-contract-multi-pl-call-fixture` (the only missing row) as a `tests-e2e-rust` dev-dependency in `tests-e2e-rust/Cargo.toml`, and add `tests-e2e-rust/tests/multi_pl_call_fixture.rs` referencing it so the existing `cargo build -p tests-e2e-rust --tests` gate compiles it. (Listing the dev-dep is what pulls it into that build, per the comment at `Cargo.toml:69-72`; the test file keeps the reference honest.) Then regenerate and commit `Cargo.lock`: the new dev-dependency edge adds one line (`"compact-contract-multi-pl-call-fixture",`) to the `tests-e2e-rust` package's dependency list, and without it every `--locked` gate (1.1d and the matrix at `rust-runtime-test.yml:161`) fails. Verify: `cargo build -p tests-e2e-rust --tests --locked` compiles it.
- [x] 2.3 Make `rustfmt` absence loud at the gate without changing the emitter: keep the soft-fail post-pass in `compiler/passes.ss:188-201` (hard-failing there would break `./compiler/go`, whose `print-rust` tests at `compiler/test.ss:82862+` run in the no-Rust-toolchain `.#compiler` devshell), and instead add an explicit `rustfmt --version` guard step to the `compiler-backed` job (see 1.1) so a missing/mismatched formatter fails with a clear message rather than surfacing as format-only byte drift across every fixture. Verify: removing `rustfmt` from the lane fails the guard with a clear message; `nix build .#compactc && cargo test -p tests-e2e-rust` still behaves as before.

## 3. Docs

- [x] 3.1 Correct AGENT.md §5.1 step 3 and §3.3 to state the dev-dependency requirement (workspace membership alone does not compile the crate in CI) and the new `compiler-backed` lane. Also correct the stale rustfmt claims in §2 rule 3 and §4: they assert a "nix-nightly rustfmt" and a `.rustfmt.toml`, but `flake.nix` ships no Rust toolchain and no `.rustfmt.toml` exists — the lane's rustup `rustfmt` is the authority. Verify: AGENT.md's recipe matches the enforced invariant and no longer names a formatter the repo does not provide.
- [x] 3.2 Record in the workflow comment that artifact sharing from `build-compiler.yml` is the planned follow-up (#23). Verify: comment present.
