# Tasks: add-digital-passport-dogfood-fixture

Prerequisites merged: `compiler-backed-ci-gate`, `vendor-digital-passport-harness`, `type-directed-expression-coercion`, `fix-ternary-expression-codegen`. Includes one small `rust-passes` codegen fix that the dogfood clippy gate surfaced (task 3.3), so the release carries a `compiler-version.ss` bump (0.31.118 → 0.31.119) + CHANGELOG rather than `skip-changelog`. All work inside `nix develop`.

## 1. Generate and register the fixture crate

- [x] 1.1 Generate: `result/bin/compactc --target rust --skip-zk examples/dogfood/digital-passport-credential/src/digital-passport-credential.compact tests-e2e-rust/contracts/digital-passport-credential/`. Verify: `lib.rs` emitted, rustfmt-clean, zero `unimplemented!`/`todo!`.
- [x] 1.2 Register: root `Cargo.toml` workspace member, `tests-e2e-rust/Cargo.toml` **dev-dependency**, `codegen_regression.rs` FIXTURES row `("dogfood/digital-passport-credential/src/digital-passport-credential.compact", "digital-passport-credential")`, and commit the updated `Cargo.lock`. Verify: `cargo build -p tests-e2e-rust --tests --locked` compiles it; the gate-invariant test from `compiler-backed-ci-gate` passes.
- [x] 1.3 Byte-parity: `cargo test -p tests-e2e-rust rust_codegen_byte_parity` green (the dogfood row regenerates byte-identically). Verify: full FIXTURES table green.

## 2. Compile requirement and behaviour parity

- [x] 2.1 Confirm the whole-entry gate was already flipped to a hard pass by `fix-ternary-expression-codegen` (task 3.3), so the entry now compiles under `--target rust`; this change no longer owns that flip. Additionally verify `--target ts --skip-zk` compiles the entry. Verify: the acceptance gate is green and no longer asserts a pre-fix refusal.
  - Recorded: the gate is now `rejection_corpus::rust_backend_dogfood_entry_compiles` (acceptance: exit 0 + `contract/lib.rs` emitted + no `pure-circuit-body-emission`); it no longer names `add-digital-passport-dogfood-fixture` as the flip owner. Ran `cargo test -p tests-e2e-rust --test rejection_corpus rust_backend_dogfood_entry_compiles` → green. `result/bin/compactc --target rust --skip-zk <entry>` exits 0, emits `contract/lib.rs` with zero `unimplemented!`/`todo!`; `--target ts --skip-zk <entry>` exits 0 and emits `contract/index.js|.d.ts|.js.map` + `compiler/contract-info.json`. (The TS smoke also becomes a permanent CI step in task 3.2.)
- [x] 2.2 Write `tests-e2e-rust/tests/digital_passport_credential.rs` (Apache header) asserting Rust outcomes equal the change-2 TS reference at each step (civil-date helpers incl. assert-fail paths, plus the protocol round-trip), comparing the round-trip's 32-byte body roots byte-for-byte. Verify: `cargo test -p tests-e2e-rust digital_passport` green; an induced wrong-width/literal bug makes it red.
  - Recorded: added `tests/digital_passport_credential.rs` (two tests: `digital_passport_civil_date_helper_parity` replays all 25 civil-date scenarios through `assert_valid_digital_passport_age_predicate`, matching the outcome and exact `failed assert: …` message per scenario; `digital_passport_round_trip_parity` rebuilds the round-trip and compares the three derived roots byte-for-byte, then runs the ten validator circuits) plus the `tests-e2e-rust/src/digital_passport_credential.rs` serde reference shape re-exported from `lib.rs`. `cargo test -p tests-e2e-rust digital_passport` green; inducing a wrong literal (`date_of_birth_days` 7400→7401) makes `credentialBodyRoot` mismatch the captured hex and the gate go red.

## 3. CI wiring

- [x] 3.1 `rust-runtime-test.yml`: add a crate-specific clippy step (`cargo clippy -p compact-contract-digital-passport-credential --all-targets --all-features`) preceded by a guard that strips the emitter's `#![allow(clippy::all, …)]` line from *this job's checkout only*, so the step actually lints rather than passing vacuously. Verify: the step lints the crate (not vacuously green).
  - Recorded: added two `pre-check` steps to `rust-runtime-test.yml`: "Re-arm clippy for the dogfood crate" (`grep -q` guard → `sed -i` on the job's checkout; exits non-zero if the emitted `#![allow(…)]` shape changes) and "Run Clippy on compact-contract-digital-passport-credential". The step runs at **default lint levels**, not `-- -D warnings`: the generated crate carries ~170 deliberate warn-level findings and its crate-level group-allow cannot be overridden by CLI deny flags (rust-lang/rust#81670), so `-D warnings` is infeasible; the deny-by-default CORRECTNESS lints gate instead. Verified: with the allow stripped, clippy reports 167 warnings and (after task 3.3) exits 0.
- [x] 3.2 `build-compiler.yml` smoke step: add `nix develop .#compiler --command compactc --target ts --skip-zk …` and the `--target rust` twin for the dogfood entry. Verify: workflow YAML lints; codegen-only (no cargo on that lane).
  - Recorded: added the two `compactc` invocations (plus a comment noting this lane is codegen-only) to the "Execute binary - smoke test" step in `build-compiler.yml`. Verified locally against the Nix-built compiler: `--target ts --skip-zk <entry> /tmp/dg-ts/` exits 0 and emits `contract/index.js|.d.ts|.js.map` + `compiler/contract-info.json`; `--target rust --skip-zk <entry> /tmp/dg-rust/` exits 0 and emits `contract/lib.rs` + `compiler/contract-info.json` with zero `unimplemented!`/`todo!`. Both workflow files parse as valid YAML.
- [x] 3.3 Fold in the codegen fix the clippy gate surfaced: `--target rust` emitted an always-false `if _repr.len() < Self::FIELD_SIZE { return None; }` guard in the zero-field `FromFieldRepr` scaffold (clippy's deny-by-default `absurd_extreme_comparisons`). Emit the guard only for structs with ≥1 field (`compiler/rust-passes-decls.ss`); regenerate the dogfood fixture; bump the compiler version (`compiler-version.ss`, `flake.nix`, `doc/ledger-adt.mdx`) + CHANGELOG. Verify: crate clippy green (exit 0); `cargo test -p tests-e2e-rust rust_codegen_byte_parity` green; compiler-version embed sweep clean.
  - Recorded: wrapped the guard emission in `(unless (null? type*) …)` in `rust-passes-decls.ss`; rebuilt `nix build .#compactc`; regenerated `tests-e2e-rust/contracts/digital-passport-credential/lib.rs` (diff = exactly the three zero-field guards removed: `NoPublicClaims`, `NoClaimCommitments`, `NoStatusBinding` — no other fixture or snapshot changes); `cargo test -p tests-e2e-rust --test codegen_regression` green; crate clippy (stripped) exits 0. Bumped `compiler-version.ss` 0.31.118 → 0.31.119 with `flake.nix` (`packages.compactc.version`) and `doc/ledger-adt.mdx`; added the 0.31.119 CHANGELOG entry. Embed sweep (`grep -rln 0.31.118` outside build dirs) shows only the archived change's own notes.

## 4. ADR, AGENT.md, gates

- [x] 4.1 New ADR (next number): dogfood enclave rationale, bounds (`examples/dogfood/` only), explicit supersession of `08decb1`'s stance for this enclave, and the pinning/refresh policy; cross-reference ADR-0001 and `08decb1`. Verify: discoverable and bounded.
  - Recorded: new `docs/adr/0003-bounded-dogfood-enclave.md` (0003 is the next number after 0001/0002, and is the number already cited by `CHANGELOG.md` and the enclave's `PROVENANCE.md`). Records the rationale (dogfooding a real, named production contract), the bounds (third-party material only under `examples/dogfood/`, enforced by `header_config.json`'s `dogfood` `excluded_directories` entry), the explicit scoped supersession of `08decb1`'s de-branding for this enclave only (the corpus stays third-party-free outside it), verbatim/compact-only vendoring, and the pin-to-`cdeb860b` + explicit-human-refresh policy. Cross-references ADR 0001 and `08decb1`. Verify: discoverable (AGENT.md §1 and `PROVENANCE.md` link to it) and bounded (directory-level exclusion, no per-file allow-list).
- [x] 4.2 AGENT.md §1: document the `examples/dogfood/` category (one paragraph; header exclusion + PROVENANCE refresh pointer). Verify: a new contributor understands the category and why it does not reverse corpus neutrality.
  - Recorded: added one paragraph to AGENT.md §1 (after the repo-shape bullets) describing the bounded third-party enclave under `examples/dogfood/`: the `dogfood` `excluded_directories` license-header exemption (upstream Apache-2.0 headers stay verbatim; every file outside still needs ours), the `PROVENANCE.md` refresh pointer (explicit human re-sync), and that it narrows — does not reverse — the `08decb1` corpus de-branding, cross-linked to ADR 0003.
- [x] 4.3 Full local gates before push: fmt, clippy (incl. the new crate), `cargo test -p midnight-compact-runtime -p tests-e2e-rust`, `add_headers.py --validate`. Verify: all green under `nix develop`, and the byte-parity + rejection gates run in the new CI lane.
  - Recorded: all gates green under `nix develop` with the repo's pinned Rust 1.95.0 toolchain (`target/` was built with rustc 1.95.0; `flake.nix` ships no toolchain, so it is put on PATH explicitly for the run). `cargo fmt --all --check` → exit 0. `cargo clippy -p midnight-compact-runtime -p midnight-compact-runtime-macros -p tests-e2e-rust --all-targets --all-features -- -D warnings` → exit 0. The dogfood crate clippy (`cargo clippy -p compact-contract-digital-passport-credential --all-targets --all-features`) with the emitted `#![allow(clippy::all, …)]` line stripped from a checkout copy (the CI re-arm) at default lint levels → exit 0 (167 warnings; `lib.rs` restored byte-identically afterwards). `cargo test -p midnight-compact-runtime -p tests-e2e-rust` → exit 0, including `codegen_regression::rust_codegen_byte_parity_against_committed_fixtures`, `every_fixture_crate_is_a_dev_dependency`, `rejection_corpus::rust_backend_dogfood_entry_compiles`, and the new `digital_passport_credential` parity tests. `python add_headers.py --validate` → no tracked file missing a header (the only misses are under gitignored generated trees: `compiler/javascript-code/`, `compiler/testdir/`, `.ptah/`, `.helix/`, `test-center/test.ts`). Rebuilt `nix build .#compactc` (0.31.119) first so the stale `result` (0.31.118) could not cause false byte-parity drift. The byte-parity + rejection gates run unskipped in the new `compiler-backed` CI lane.
- [x] 4.4 Commit signed+DCO on `feature/add-digital-passport-dogfood-fixture`. Verify: `git log --show-signature` clean; vendored bytes unchanged by the commit.
  - Recorded: committed signed + DCO on the change's branch. Harness note: the OpenSpec harness cut this change's working branch as `opsx-add-digital-passport-dogfood-fixture` (the branch the whole change, including tasks 1.x–3.3, was implemented on, based on the merged prerequisites #77–#80); the plan's literal `feature/add-digital-passport-dogfood-fixture` name is a stale branch that predates the prerequisite merges and does not contain them, so the coherent target is the harness branch. Verify: `git log --format='%h %G? %s' -1` shows `G` (good/valid signature) with a `Signed-off-by:` trailer, and the commit touches no path under `examples/dogfood/` (vendored bytes unchanged).
