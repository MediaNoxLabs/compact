// This file is part of Compact.
// Copyright (C) 2026 Midnight Foundation
// SPDX-License-Identifier: Apache-2.0
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//  	http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// Generated by compactc. Do not edit by hand.

#![allow(
    clippy::all,
    dead_code,
    unused_imports,
    unused_variables,
    unused_parens,
    non_camel_case_types,
    non_snake_case,
    non_upper_case_globals
)]

use midnight_compact_runtime::*;
use std::marker::PhantomData;

midnight_compact_runtime::check_runtime_version!("0.16.100");

#[derive(Clone, Copy, Debug, PartialEq, Eq, Default)]
#[repr(u8)]
pub enum Color {
    #[default]
    red = 0,
    green = 1,
    blue = 2,
}
impl Aligned for Color {
    fn alignment() -> Alignment {
        u8::alignment()
    }
}
impl FieldRepr for Color {
    fn field_repr<W: MemWrite<Fr>>(&self, writer: &mut W) {
        (*self as u8).field_repr(writer);
    }
    fn field_size(&self) -> usize {
        1
    }
}
impl FromFieldRepr for Color {
    const FIELD_SIZE: usize = 1;
    fn from_field_repr(r: &[Fr]) -> Option<Self> {
        let n = u8::from_field_repr(r)?;
        match n {
            0 => Some(Self::red),
            1 => Some(Self::green),
            2 => Some(Self::blue),
            _ => None,
        }
    }
}
impl From<Color> for midnight_compact_runtime::Value {
    fn from(v: Color) -> midnight_compact_runtime::Value {
        midnight_compact_runtime::Value::from(v as u8)
    }
}
impl midnight_compact_runtime::BinaryHashRepr for Color {
    fn binary_repr<W: MemWrite<u8>>(&self, writer: &mut W) {
        (*self as u8).binary_repr(writer);
    }
    fn binary_len(&self) -> usize {
        1
    }
}
#[derive(Clone, Debug, PartialEq, Eq, Default)]
pub struct Box {
    pub f: Fr,
}
impl Aligned for Box {
    fn alignment() -> Alignment {
        Alignment::concat([&<Fr as Aligned>::alignment()])
    }
}
impl FieldRepr for Box {
    fn field_repr<W: MemWrite<Fr>>(&self, writer: &mut W) {
        self.f.field_repr(writer);
    }
    fn field_size(&self) -> usize {
        self.f.field_size()
    }
}
impl FromFieldRepr for Box {
    const FIELD_SIZE: usize = <Fr as FromFieldRepr>::FIELD_SIZE;
    fn from_field_repr(_repr: &[Fr]) -> Option<Self> {
        if _repr.len() < Self::FIELD_SIZE {
            return None;
        }
        let mut _offset = 0usize;
        let f = <Fr as FromFieldRepr>::from_field_repr(
            &_repr[_offset.._offset + <Fr as FromFieldRepr>::FIELD_SIZE],
        )?;
        _offset += <Fr as FromFieldRepr>::FIELD_SIZE;
        let _ = _offset;
        Some(Box { f })
    }
}
impl From<Box> for midnight_compact_runtime::Value {
    fn from(s: Box) -> midnight_compact_runtime::Value {
        let mut _v: Vec<midnight_compact_runtime::Value> = Vec::new();
        _v.push(midnight_compact_runtime::Value::from(s.f));
        midnight_compact_runtime::Value::concat(_v.iter())
    }
}
impl midnight_compact_runtime::BinaryHashRepr for Box {
    fn binary_repr<W: MemWrite<u8>>(&self, writer: &mut W) {
        self.f.binary_repr(writer);
    }
    fn binary_len(&self) -> usize {
        self.f.binary_len()
    }
}

pub trait Witnesses<PS> {
    fn echo_field<'a>(&self, ctx: &WitnessContext<Ledger<'a>, PS>, x: Fr) -> (PS, Fr);
}

pub struct Contract<PS, W = NoWitnesses>
where
    W: Witnesses<PS>,
{
    pub witnesses: W,
    _ps: PhantomData<PS>,
}

impl<PS, W> Contract<PS, W>
where
    PS: Clone,
    W: Witnesses<PS>,
{
    pub fn new(witnesses: W) -> Self {
        Self {
            witnesses,
            _ps: PhantomData,
        }
    }
    pub fn initial_state(
        &self,
        ctx: ConstructorContext<PS>,
        c: bool,
        d: bool,
        x: Fr,
    ) -> Result<ConstructorResult<PS>, CompactError> {
        let sv = new_array(vec![
            new_cell(false),
            new_cell(Fr::default()),
            new_cell(0u64),
            new_cell_array([Fr::default(); 2]),
            new_cell(0u64),
        ]);
        let state = ChargedState::new(sv);
        let qctx = QueryContext::new(state, midnight_compact_runtime::ContractAddress::default());
        let a = if c { 1u8 } else { 2u8 };
        let y = if c { x } else { Fr::from(0u64) };
        let b = Box {
            f: Fr::from((if c { 1u8 } else { 2u8 }) as u64),
        };
        let z = if c {
            if d {
                1u8
            } else {
                2u8
            }
        } else {
            if d {
                3u8
            } else {
                4u8
            }
        };
        let p =
            midnight_compact_runtime::hash_to_curve(Fr::from((if c { 3u8 } else { 4u8 }) as u64));
        let r = pure_circuits::idf(Fr::from((if c { 7u8 } else { 8u8 }) as u64))?;
        let cmp = (0 == if c { 1u8 } else { 0u8 });
        compact_assert!(if c { (x == x) } else { (x == x) }, "ctor ternary assert");
        let tmp = ((((((y) + (b.f)) + (Fr::from((z) as u64)))
            + (midnight_compact_runtime::jubjub_point_x(p.clone())))
            + (Fr::from((a) as u64)))
            + (r))
            + (Fr::from((if cmp { 1u8 } else { 0u8 }) as u64));
        let tmp_0 = ((if c { 10u8 } else { 20u8 }) as u64);
        let tmp_1 = [
            Fr::from((if c { 1u8 } else { 2u8 }) as u64),
            Fr::from((if c { 3u8 } else { 4u8 }) as u64),
        ];
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp.clone()))
            .ins(false, 1)
            .push(false, new_cell(2u8))
            .push(true, new_cell(tmp_0.clone()))
            .ins(false, 1)
            .push(false, new_cell(3u8))
            .push(true, new_cell_array(tmp_1.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(&qctx, &ops, ctx.gas_limit.clone(), &ctx.cost_model)?;

        Ok(ConstructorResult {
            current_contract_state: results.context.state,
            current_private_state: ctx.initial_private_state,
            current_zswap_local_state: ctx.empty_zswap_local_state,
        })
    }

    pub fn walker_const_annotated(
        &self,
        ctx: CircuitContext<PS>,
        c: bool,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let x = if c { 1u8 } else { 2u8 };
        let tmp = Fr::from((x) as u64).clone();
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn walker_compare_eq(
        &self,
        ctx: CircuitContext<PS>,
        c: bool,
        x: u8,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let b = (x == if c { 1u8 } else { 0u8 });
        let tmp = if b { Fr::from(1u64) } else { Fr::from(0u64) };
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn walker_call_pure(
        &self,
        ctx: CircuitContext<PS>,
        c: bool,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let tmp = pure_circuits::idf(Fr::from((if c { 1u8 } else { 2u8 }) as u64))?;
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn walker_struct_member(
        &self,
        ctx: CircuitContext<PS>,
        c: bool,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let b = Box {
            f: Fr::from((if c { 1u8 } else { 2u8 }) as u64),
        };
        let tmp = b.f.clone();
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn walker_write(
        &self,
        ctx: CircuitContext<PS>,
        c: bool,
        x: Fr,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let tmp = if c { x } else { Fr::from(0u64) };
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn witness_arg(
        &self,
        ctx: CircuitContext<PS>,
        c: bool,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let _witness_ctx_0 = WitnessContext::new(
            ledger(&ctx.current_query_context.state),
            ctx.current_private_state,
            &ctx.current_query_context,
        );
        let (current_private_state, w) = self.witnesses.echo_field(
            &_witness_ctx_0,
            Fr::from((if c { 1u8 } else { 2u8 }) as u64),
        );
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(w))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                current_private_state,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn stream_increment(
        &self,
        ctx: CircuitContext<PS>,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let f = {
            let _gather_ops = OpProgramGather::<DefaultDB>::new()
                .dup(0)
                .idx_at_index(0u8, false)
                .popeq(false)
                .build();
            let _gather_results = query_for_read(
                &ctx.current_query_context,
                &_gather_ops,
                None,
                &initial_cost_model(),
            )
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
            let _av = match _gather_results.events.last() {
                Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => {
                    av
                }
                _ => {
                    return Err(CompactError::AssertionFailed(
                        "ledger: expected Read event".into(),
                    ))
                }
            };
            midnight_compact_runtime::std_lib::decode_bool(_av)?
        };
        let tmp = ((if f { 3u8 } else { 4u8 }) as u16);
        let tmp_0 = ((if f { 10u8 } else { 20u8 }) as u64);
        let ops = OpProgramVerify::<DefaultDB>::new()
            .idx_at_index(4u8, true)
            .addi(tmp.clone() as u32)
            .ins(true, 1)
            .push(false, new_cell(2u8))
            .push(true, new_cell(tmp_0.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn stream_compare_eq(
        &self,
        ctx: CircuitContext<PS>,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let f = {
            let _gather_ops = OpProgramGather::<DefaultDB>::new()
                .dup(0)
                .idx_at_index(0u8, false)
                .popeq(false)
                .build();
            let _gather_results = query_for_read(
                &ctx.current_query_context,
                &_gather_ops,
                None,
                &initial_cost_model(),
            )
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
            let _av = match _gather_results.events.last() {
                Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => {
                    av
                }
                _ => {
                    return Err(CompactError::AssertionFailed(
                        "ledger: expected Read event".into(),
                    ))
                }
            };
            midnight_compact_runtime::std_lib::decode_bool(_av)?
        };
        let b = (1 == if f { 1u8 } else { 0u8 });
        let tmp = if b { Fr::from(1u64) } else { Fr::from(0u64) };
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn stream_write(
        &self,
        ctx: CircuitContext<PS>,
        c: bool,
        x: Fr,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let tmp = ((if c { 1u8 } else { 2u8 }) as u16);
        let ops = OpProgramVerify::<DefaultDB>::new()
            .idx_at_index(4u8, true)
            .addi(tmp.clone() as u32)
            .ins(true, 1)
            .push(false, new_cell(1u8))
            .push(true, new_cell(x))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn walker_vector_element(
        &self,
        ctx: CircuitContext<PS>,
        c: bool,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let tmp = [
            Fr::from((if c { 1u8 } else { 2u8 }) as u64),
            Fr::from((if c { 3u8 } else { 4u8 }) as u64),
        ];
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(3u8))
            .push(true, new_cell_array(tmp.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn walker_native_arg(
        &self,
        ctx: CircuitContext<PS>,
        c: bool,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let p =
            midnight_compact_runtime::hash_to_curve(Fr::from((if c { 1u8 } else { 2u8 }) as u64));
        let tmp = midnight_compact_runtime::jubjub_point_x(p.clone());
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn walker_nested_if(
        &self,
        ctx: CircuitContext<PS>,
        c: bool,
        d: bool,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let x = if c {
            if d {
                1u8
            } else {
                2u8
            }
        } else {
            if d {
                3u8
            } else {
                4u8
            }
        };
        let tmp = Fr::from((x) as u64).clone();
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn walker_inline_write(
        &self,
        ctx: CircuitContext<PS>,
        c: bool,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let tmp = ((if c { 1u8 } else { 2u8 }) as u16);
        let ops = OpProgramVerify::<DefaultDB>::new()
            .idx_at_index(4u8, true)
            .addi(tmp.clone() as u32)
            .ins(true, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn stream_call_pure(
        &self,
        ctx: CircuitContext<PS>,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let f = {
            let _gather_ops = OpProgramGather::<DefaultDB>::new()
                .dup(0)
                .idx_at_index(0u8, false)
                .popeq(false)
                .build();
            let _gather_results = query_for_read(
                &ctx.current_query_context,
                &_gather_ops,
                None,
                &initial_cost_model(),
            )
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
            let _av = match _gather_results.events.last() {
                Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => {
                    av
                }
                _ => {
                    return Err(CompactError::AssertionFailed(
                        "ledger: expected Read event".into(),
                    ))
                }
            };
            midnight_compact_runtime::std_lib::decode_bool(_av)?
        };
        let tmp = pure_circuits::idf(Fr::from((if f { 1u8 } else { 2u8 }) as u64))?;
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn stream_vector_element(
        &self,
        ctx: CircuitContext<PS>,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let f = {
            let _gather_ops = OpProgramGather::<DefaultDB>::new()
                .dup(0)
                .idx_at_index(0u8, false)
                .popeq(false)
                .build();
            let _gather_results = query_for_read(
                &ctx.current_query_context,
                &_gather_ops,
                None,
                &initial_cost_model(),
            )
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
            let _av = match _gather_results.events.last() {
                Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => {
                    av
                }
                _ => {
                    return Err(CompactError::AssertionFailed(
                        "ledger: expected Read event".into(),
                    ))
                }
            };
            midnight_compact_runtime::std_lib::decode_bool(_av)?
        };
        let tmp = [
            Fr::from((if f { 1u8 } else { 2u8 }) as u64),
            Fr::from((if f { 3u8 } else { 4u8 }) as u64),
        ];
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(3u8))
            .push(true, new_cell_array(tmp.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn stream_native_arg(
        &self,
        ctx: CircuitContext<PS>,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let f = {
            let _gather_ops = OpProgramGather::<DefaultDB>::new()
                .dup(0)
                .idx_at_index(0u8, false)
                .popeq(false)
                .build();
            let _gather_results = query_for_read(
                &ctx.current_query_context,
                &_gather_ops,
                None,
                &initial_cost_model(),
            )
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
            let _av = match _gather_results.events.last() {
                Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => {
                    av
                }
                _ => {
                    return Err(CompactError::AssertionFailed(
                        "ledger: expected Read event".into(),
                    ))
                }
            };
            midnight_compact_runtime::std_lib::decode_bool(_av)?
        };
        let p =
            midnight_compact_runtime::hash_to_curve(Fr::from((if f { 1u8 } else { 2u8 }) as u64));
        let tmp = midnight_compact_runtime::jubjub_point_x(p.clone());
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn stream_struct_member(
        &self,
        ctx: CircuitContext<PS>,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let f = {
            let _gather_ops = OpProgramGather::<DefaultDB>::new()
                .dup(0)
                .idx_at_index(0u8, false)
                .popeq(false)
                .build();
            let _gather_results = query_for_read(
                &ctx.current_query_context,
                &_gather_ops,
                None,
                &initial_cost_model(),
            )
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
            let _av = match _gather_results.events.last() {
                Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => {
                    av
                }
                _ => {
                    return Err(CompactError::AssertionFailed(
                        "ledger: expected Read event".into(),
                    ))
                }
            };
            midnight_compact_runtime::std_lib::decode_bool(_av)?
        };
        let b = Box {
            f: Fr::from((if f { 1u8 } else { 2u8 }) as u64),
        };
        let tmp = b.f.clone();
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn stream_call_witness(
        &self,
        ctx: CircuitContext<PS>,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let f = {
            let _gather_ops = OpProgramGather::<DefaultDB>::new()
                .dup(0)
                .idx_at_index(0u8, false)
                .popeq(false)
                .build();
            let _gather_results = query_for_read(
                &ctx.current_query_context,
                &_gather_ops,
                None,
                &initial_cost_model(),
            )
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
            let _av = match _gather_results.events.last() {
                Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => {
                    av
                }
                _ => {
                    return Err(CompactError::AssertionFailed(
                        "ledger: expected Read event".into(),
                    ))
                }
            };
            midnight_compact_runtime::std_lib::decode_bool(_av)?
        };
        let _witness_ctx_1 = WitnessContext::new(
            ledger(&ctx.current_query_context.state),
            ctx.current_private_state,
            &ctx.current_query_context,
        );
        let (current_private_state, w) = self.witnesses.echo_field(
            &_witness_ctx_1,
            Fr::from((if f { 1u8 } else { 2u8 }) as u64),
        );
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(w))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                current_private_state,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn stream_const_annotated(
        &self,
        ctx: CircuitContext<PS>,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let f = {
            let _gather_ops = OpProgramGather::<DefaultDB>::new()
                .dup(0)
                .idx_at_index(0u8, false)
                .popeq(false)
                .build();
            let _gather_results = query_for_read(
                &ctx.current_query_context,
                &_gather_ops,
                None,
                &initial_cost_model(),
            )
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
            let _av = match _gather_results.events.last() {
                Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => {
                    av
                }
                _ => {
                    return Err(CompactError::AssertionFailed(
                        "ledger: expected Read event".into(),
                    ))
                }
            };
            midnight_compact_runtime::std_lib::decode_bool(_av)?
        };
        let x = ((if f { 1u8 } else { 2u8 }) as u16);
        let tmp = Fr::from(1u64);
        let ops = OpProgramVerify::<DefaultDB>::new()
            .idx_at_index(4u8, true)
            .addi(x.clone() as u32)
            .ins(true, 1)
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn stream_assert_eq(
        &self,
        ctx: CircuitContext<PS>,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let f = {
            let _gather_ops = OpProgramGather::<DefaultDB>::new()
                .dup(0)
                .idx_at_index(0u8, false)
                .popeq(false)
                .build();
            let _gather_results = query_for_read(
                &ctx.current_query_context,
                &_gather_ops,
                None,
                &initial_cost_model(),
            )
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
            let _av = match _gather_results.events.last() {
                Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => {
                    av
                }
                _ => {
                    return Err(CompactError::AssertionFailed(
                        "ledger: expected Read event".into(),
                    ))
                }
            };
            midnight_compact_runtime::std_lib::decode_bool(_av)?
        };
        compact_assert!(if f { (1 == 1) } else { (2 == 2) }, "stream ternary assert");
        let tmp = Fr::from(1u64);
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp.clone()))
            .ins(false, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }

    pub fn stream_nested_if(
        &self,
        ctx: CircuitContext<PS>,
    ) -> Result<CircuitResults<PS, ()>, CompactError> {
        let f = {
            let _gather_ops = OpProgramGather::<DefaultDB>::new()
                .dup(0)
                .idx_at_index(0u8, false)
                .popeq(false)
                .build();
            let _gather_results = query_for_read(
                &ctx.current_query_context,
                &_gather_ops,
                None,
                &initial_cost_model(),
            )
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
            let _av = match _gather_results.events.last() {
                Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => {
                    av
                }
                _ => {
                    return Err(CompactError::AssertionFailed(
                        "ledger: expected Read event".into(),
                    ))
                }
            };
            midnight_compact_runtime::std_lib::decode_bool(_av)?
        };
        let x = if f {
            if f {
                1u8
            } else {
                2u8
            }
        } else {
            if f {
                3u8
            } else {
                4u8
            }
        };
        let tmp = Fr::from((x) as u64).clone();
        let tmp_0 = 1u16;
        let ops = OpProgramVerify::<DefaultDB>::new()
            .push(false, new_cell(1u8))
            .push(true, new_cell(tmp.clone()))
            .ins(false, 1)
            .idx_at_index(4u8, true)
            .addi(tmp_0.clone() as u32)
            .ins(true, 1)
            .build();

        let results = query_for_verify(
            &ctx.current_query_context,
            &ops,
            ctx.gas_limit.clone(),
            &ctx.cost_model,
        )?;

        Ok(CircuitResults {
            result: (),
            context: CircuitContext {
                current_query_context: results.context,
                ..ctx
            },
            gas_cost: results.gas_cost,
        })
    }
}

pub struct Ledger<'a, D: DB = DefaultDB> {
    state: &'a ChargedState<D>,
}

pub fn ledger<D: DB>(state: &ChargedState<D>) -> Ledger<'_, D> {
    Ledger { state }
}

impl<'a, D: DB> Ledger<'a, D> {
    pub fn flag(&self) -> Result<bool, CompactError> {
        let qctx = QueryContext::new(
            self.state.clone(),
            midnight_compact_runtime::ContractAddress::default(),
        );
        let ops = OpProgramGather::<D>::new()
            .dup(0)
            .idx_at_index(0u8, false)
            .popeq(true)
            .build();
        let results = query_for_read(&qctx, &ops, None, &initial_cost_model())
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
        let av = match results.events.last() {
            Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => av,
            _ => {
                return Err(CompactError::AssertionFailed(
                    "ledger: expected Read event".into(),
                ))
            }
        };
        midnight_compact_runtime::std_lib::decode_bool(av)
    }
    pub fn field_cell(&self) -> Result<Fr, CompactError> {
        let qctx = QueryContext::new(
            self.state.clone(),
            midnight_compact_runtime::ContractAddress::default(),
        );
        let ops = OpProgramGather::<D>::new()
            .dup(0)
            .idx_at_index(1u8, false)
            .popeq(true)
            .build();
        let results = query_for_read(&qctx, &ops, None, &initial_cost_model())
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
        let av = match results.events.last() {
            Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => av,
            _ => {
                return Err(CompactError::AssertionFailed(
                    "ledger: expected Read event".into(),
                ))
            }
        };
        midnight_compact_runtime::std_lib::decode_fr(av)
    }
    pub fn wide_cell(&self) -> Result<u64, CompactError> {
        let qctx = QueryContext::new(
            self.state.clone(),
            midnight_compact_runtime::ContractAddress::default(),
        );
        let ops = OpProgramGather::<D>::new()
            .dup(0)
            .idx_at_index(2u8, false)
            .popeq(true)
            .build();
        let results = query_for_read(&qctx, &ops, None, &initial_cost_model())
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
        let av = match results.events.last() {
            Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => av,
            _ => {
                return Err(CompactError::AssertionFailed(
                    "ledger: expected Read event".into(),
                ))
            }
        };
        midnight_compact_runtime::std_lib::decode_u64(av)
    }
    pub fn vec_cell(&self) -> Result<[Fr; 2], CompactError> {
        let qctx = QueryContext::new(
            self.state.clone(),
            midnight_compact_runtime::ContractAddress::default(),
        );
        let ops = OpProgramGather::<D>::new()
            .dup(0)
            .idx_at_index(3u8, false)
            .popeq(true)
            .build();
        let results = query_for_read(&qctx, &ops, None, &initial_cost_model())
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
        let av = match results.events.last() {
            Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => av,
            _ => {
                return Err(CompactError::AssertionFailed(
                    "ledger: expected Read event".into(),
                ))
            }
        };
        midnight_compact_runtime::std_lib::decode_vector_fr::<2>(av)
    }
    pub fn ops(&self) -> Result<u64, CompactError> {
        let qctx = QueryContext::new(
            self.state.clone(),
            midnight_compact_runtime::ContractAddress::default(),
        );
        let ops = OpProgramGather::<D>::new()
            .dup(0)
            .idx_at_index(4u8, false)
            .popeq(true)
            .build();
        let results = query_for_read(&qctx, &ops, None, &initial_cost_model())
            .map_err(|e| CompactError::AssertionFailed(format!("ledger query failed: {:?}", e)))?;
        let av = match results.events.last() {
            Some(midnight_compact_runtime::onchain_vm::result_mode::GatherEvent::Read(av)) => av,
            _ => {
                return Err(CompactError::AssertionFailed(
                    "ledger: expected Read event".into(),
                ))
            }
        };
        midnight_compact_runtime::std_lib::decode_u64(av)
    }
}

pub mod pure_circuits {
    use super::*;

    pub fn idf(x: Fr) -> Result<Fr, CompactError> {
        Ok(x)
    }

    pub fn const_annotated_both_literal(c: bool) -> Result<u8, CompactError> {
        let x = if c { 1u8 } else { 2u8 };
        Ok(x)
    }

    pub fn const_unannotated_seq_lifted(c: bool, a: u8) -> Result<u8, CompactError> {
        let x = if c {
            {
                compact_assert!((a >= 1), "result of subtraction would be negative");
                ((a) as u8).wrapping_sub((1) as u8)
            }
        } else {
            a
        };
        Ok(x)
    }

    pub fn return_tail_mixed(c: bool, x: u8) -> Result<u8, CompactError> {
        Ok(if c { 5 } else { x })
    }

    pub fn return_tail_nested(c: bool, d: bool) -> Result<u8, CompactError> {
        Ok(if c {
            if d {
                1u8
            } else {
                2u8
            }
        } else {
            if d {
                3u8
            } else {
                4u8
            }
        })
    }

    pub fn assert_arg(c: bool, x: u8) -> Result<(), CompactError> {
        compact_assert!(if c { (x > 1) } else { (x > 2) }, "ternary assert");
        Ok(())
    }

    pub fn arith_operand(c: bool, n: u8) -> Result<u64, CompactError> {
        Ok(((((n) as u16).wrapping_add((if c { 1 } else { 0u8 }) as u16)) as u64))
    }

    pub fn cmp_operand(c: bool, x: u8) -> Result<bool, CompactError> {
        Ok((x == if c { 1u8 } else { 0u8 }))
    }

    pub fn call_arg_pure(c: bool, a: Fr, b: Fr) -> Result<Fr, CompactError> {
        Ok(pure_circuits::idf(if c { a } else { b })?)
    }

    pub fn call_arg_ctor(c: bool, a: Fr, b: Fr) -> Result<Maybe<Fr>, CompactError> {
        Ok(midnight_compact_runtime::std_lib::some(if c {
            a
        } else {
            b
        }))
    }

    pub fn struct_member(c: bool) -> Result<Box, CompactError> {
        Ok(Box {
            f: Fr::from((if c { 1u8 } else { 2u8 }) as u64),
        })
    }

    pub fn struct_valued_arms(c: bool, s1: Box, s2: Box) -> Result<Box, CompactError> {
        Ok(if c { s1 } else { s2 })
    }

    pub fn vector_element(c: bool) -> Result<[Fr; 2], CompactError> {
        Ok([
            Fr::from((if c { 1u8 } else { 2u8 }) as u64),
            Fr::from((if c { 3u8 } else { 4u8 }) as u64),
        ])
    }

    pub fn native_arg(c: bool, a: Fr, b: Fr) -> Result<JubjubPoint, CompactError> {
        Ok(midnight_compact_runtime::hash_to_curve(if c {
            a
        } else {
            b
        }))
    }

    pub fn field_typed_arms(c: bool, f1: Fr, f2: Fr) -> Result<Fr, CompactError> {
        Ok(if c { f1 } else { f2 })
    }

    pub fn enum_valued(c: bool) -> Result<Color, CompactError> {
        Ok(if c { Color::red } else { Color::green })
    }

    pub fn literal_above_i32(c: bool) -> Result<u64, CompactError> {
        let x = ((if c { 3000000000u32 } else { 0u32 }) as u64);
        Ok(x)
    }

    pub fn literal_above_u64(c: bool) -> Result<u128, CompactError> {
        let x = if c {
            340282366920938463463374607431768211455u128
        } else {
            0u128
        };
        Ok(x)
    }

    pub fn differing_uint_widths(c: bool, a: u8, b: u32) -> Result<u32, CompactError> {
        Ok(if c { ((a) as u32) } else { b })
    }

    pub fn uint_arm_into_field(c: bool, a: u8, b: u8) -> Result<Fr, CompactError> {
        Ok(Fr::from((if c { a } else { b }) as u64))
    }

    pub fn pick(c: u8) -> Result<u8, CompactError> {
        Ok(if (c > 5) {
            compact_assert!((c >= 10), "result of subtraction would be negative");
            ((c) as u8).wrapping_sub((10) as u8)
        } else {
            c
        })
    }

    pub fn walker_return_tail(c: bool) -> Result<Fr, CompactError> {
        Ok(Fr::from((if c { 1u8 } else { 2u8 }) as u64))
    }

    pub fn walker_call_ctor(c: bool) -> Result<Maybe<Fr>, CompactError> {
        Ok(midnight_compact_runtime::std_lib::some(Fr::from(
            (if c { 1u8 } else { 2u8 }) as u64,
        )))
    }
}
