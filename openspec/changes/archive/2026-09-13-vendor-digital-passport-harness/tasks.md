# Tasks: vendor-digital-passport-harness

No compiler change; request the maintainer-applied `skip-changelog` label. All work inside `nix develop`.

## 1. Vendor the Compact sources

- [x] 1.1 Clone upstream at rev `cdeb860b` and copy only `packages/midnight-verifiable-credential-digital-passport/src/**.compact` → `examples/dogfood/digital-passport-credential/src/` (6 files: entry + five modules), verbatim with upstream headers intact. Vendor **no** `.ts` or other files. Verify: per-file `cmp` against the upstream clone; the vendored `src/**/*.compact` file list equals upstream's; `find examples/dogfood -name '*.ts' | wc -l` is `0`.
- [x] 1.2 Stage the core without pnpm: `curl -L $(npm view @midnight-ntwrk/credential-compact@0.1.0-rc3 dist.tarball) | tar xz`, copy `dist/credentials.compact` + `dist/credentials/` → `examples/dogfood/digital-passport-credential/core-compact-staging/` (15 `.compact` files). Verify: file count is 15 and bytes match the tarball.
- [x] 1.3 Write `examples/dogfood/digital-passport-credential/PROVENANCE.md`: upstream URL, rev `cdeb860b`, license, npm core package+version, staging procedure (incl. the no-pnpm variant), refresh policy (explicit manual re-sync; rev updated in the same commit), and the compact-only / no-TypeScript statement. Verify: a reader can re-sync from PROVENANCE alone.
- [x] 1.4 Run `git status --ignored` over the vendored tree and confirm every file is tracked. Verify: `git ls-files examples/dogfood | wc -l` is 22 (6 + 15 + `PROVENANCE.md`).

## 2. Header exclusion + compile smoke

- [x] 2.1 Add the `dogfood` entry to `header_config.json` `excluded_directories`. Verify: `python add_headers.py --validate` passes with the vendored tree present.
- [x] 2.2 Local compile smoke: `result/bin/compactc --target ts --skip-zk examples/dogfood/digital-passport-credential/src/digital-passport-credential.compact /tmp/out-ts/` exits 0. Verify: TS output generated. (The rust target is expected to fail until the fix changes; record the exact diagnostics.)
  - Recorded pre-fix TS target: exit 0; emitted `contract/index.js`, `contract/index.d.ts`, `contract/index.js.map`, `compiler/contract-info.json`.
  - Recorded pre-fix rust target (used by task 4.3's gate): exit **255**, no `contract/lib.rs` emitted. Kind **`pure-circuit-body-emission`**, at `src/digital-passport-credential/helpers.compact:257:1` (`pure circuit assertCivilDateMatchesEpochDays`). Exact message: `compactc --target rust: unsupported Compact construct (pure-circuit-body-emission): no walker shape matched pure circuit body for assertCivilDateMatchesEpochDays`. This is the ternary gap `fix-ternary-expression-codegen` documents (the Rust `expr-rust` has no `if` arm, so guarded callers report the swallowed `no walker shape matched` body error).

## 3. TS reference captures

- [x] 3.1 Author `tests-e2e-rust/fixtures/capture-digital-passport-credential.mjs` (Apache header) per the existing capture pattern: civil-date helpers (`assertCivilDateMatchesEpochDays`, `assertValidDigitalPassportAgePredicate` — every conditional-expression site and assert-fail path) + one issuance/presentation/verification round-trip. Verify: script runs under the TS target and emits `tests-e2e-rust/fixtures/digital-passport-credential-ts-state.json`.
  - Recorded: the contract is a pure helper library (no ledger/constructor/impure circuits), so the capture records per-step *outcomes* (`{ok:true}` / `{ok:false,error:<exact message>}`) rather than `stateHex`. Because `assertCivilDateMatchesEpochDays` is non-exported, every scenario drives it through the exported `assertValidDigitalPassportAgePredicate` (which calls it for both `currentDate` and `dateOfBirthDate`). 25 civil-date scenarios recorded (6 accept / 19 reject) + 13 round-trip steps, all `ok:true`. Three boundary scenarios were added in review to isolate each disjunct of the `beforeBirthdayThisYear` disjunction at the accept/reject threshold, so it cannot be dropped, negated, or widened undetected: `accept_birthday_today_at_threshold` (both disjuncts false; full year counted) flips if either disjunct widens to `<=`; `reject_before_birthday_first_disjunct` (only the first disjunct, `currentDate.month < dateOfBirthDate.month`, true) flips if that disjunct is dropped or its `<` negated to `>`; `reject_before_birthday_disjunct` (only the second disjunct true) flips if that disjunct is dropped or negated. Honest Schnorr proofs for the round-trip are produced in-script by the contract's own `issuanceProofChallenge`/`presentationProofChallenge` circuits.
- [x] 3.2 Commit the JSON reference as `tests-e2e-rust/fixtures/digital-passport-credential-ts-state.json` (the name `add-digital-passport-dogfood-fixture`'s parity test loads) and document in the change notes which helper circuits and round-trip steps it pins. Verify: the JSON covers every helper conditional site.
  - Helper circuits pinned (under `civilDateHelpers`, each entry naming its `site`): `assertCivilDateMatchesEpochDays` (both call sites via the age predicate) — every conditional-expression site: the `yearAdjusted` / `isLeap` / `shiftedMonth` ternaries (both branches), the `month == 2` leap/non-leap day bound (`? <=29 : <=28`), the 31-day/30-day month branches, and in `assertValidDigitalPassportAgePredicate` the `beforeBirthdayThisYear` disjunction and the `? 1 : 0` age correction; plus every `assert`-fail path (year/month/day bounds, the three quotient ranges, the month-day-offset range, the 31-/Feb-/30-day length checks, the `epochDays` reconstruction, the DOB-commitment match, `currentDay >= dateOfBirthDays`, `proveAgeOverThreshold`, and the age threshold).
  - Round-trip steps pinned (under `roundTrip`): the derived roots `credentialBodyRoot`, `presentationBodyRoot`, `presentationRequestBodyRoot`; the issuance sequence `assertValidDigitalPassportIssuanceOffer` → `assertValidDigitalPassportIssuanceRequest` → `assertDigitalPassportIssuanceRequestMatchesOffer` → `assertValidDigitalPassportIssuanceResult` → `assertDigitalPassportIssuanceResultMatchesRequest`; and the verification sequence `assertValidDigitalPassportVerificationRequestMessage` → `assertValidDigitalPassportVerificationSubmissionMessage` → `assertDigitalPassportVerificationSubmissionMatchesRequest` → `assertValidDigitalPassportVerificationResultMessage` → `assertDigitalPassportVerificationResultMatchesSubmission`.

## 4. Oracle probes (expected refusals)

- [x] 4.1 Add `rejection_corpus` entries for the real ternary sites (minimal extracts in each of the three syntactic positions: const RHS, assert argument, interior arithmetic operand) asserting the exact current refusal diagnostics. Verify: `cargo test -p tests-e2e-rust rejection_corpus` green with the pre-fix compiler; each entry names the fix change that flips it.
  - Recorded: three minimal extracts added to `REJECTIONS` — `ternary in const RHS`, `ternary in assert argument`, `ternary as interior arithmetic operand` (each an `export pure circuit`; a non-exported one is dead-code-elided, so it must be exported to reach emission). Each refuses with the same kind as the whole-entry failure recorded in task 2.2, **`pure-circuit-body-emission`** (exit 255, no `lib.rs`): the pure-circuit emitter renders non-call expressions under a catch-all guard that swallows the precise diagnostic and reports the generic body error. Each case name carries `[flips in fix-ternary-expression-codegen]`, the change that owns the flip (its task 3.1).
- [x] 4.2 Add a `rejection_corpus` entry for a mixed-width comparison operand (the `q * 4`-vs-`Uint<32>` shape) asserting the current refusal. Verify: green pre-fix; names `type-directed-expression-coercion` as the flipper (it owns the flip — see its task 4.4).
  - Recorded: the mixed-width refusal is **route-specific**. On the pure route `assert(q * 4 <= y)` with `q, y: Uint<32>` does NOT refuse — it exits 0 and emits `(t: u64) <= y: u32` (an `E0308` that only surfaces at `cargo build`), so it cannot be a `rejection_corpus` entry. The constructor route is where the range-widening coercion lift is unlowerable: `constructor(q: Uint<32>, y: Uint<32>) { assert(q * 4 <= y, "..."); last = disclose(y); }` refuses with **`ctor-body-emission`** (exit 255, no `lib.rs`). The uniform-width neighbour `assert(q <= y, ...)` in the SAME constructor shape ACCEPTS, so the entry keys on the mixed width; it is pinned in `ACCEPTIONS`. Case name carries `[flips in type-directed-expression-coercion]`.
- [x] 4.3 Add a whole-entry expected-failure gate in `tests-e2e-rust/tests/rejection_corpus.rs` as a `rust_backend_*` test (so the `compiler-backed` lane runs it). Unlike the inline `REJECTIONS` cases, it MUST compile the vendored entry **in place** by repo-relative path: add a `compile_repo_relative(source_path)` helper mirroring `codegen_regression.rs`'s invocation (`Command::new(compactc)` with `--target rust --skip-zk <src> <outdir>`), because the existing `compile()` writes its probe to a temp dir and breaks the entry's `include ../core-compact-staging/...` resolution. Assert: exit non-zero, no `contract/lib.rs` emitted, and the diagnostic names the kind recorded by task 2.2. Designed to flip to "exits 0 + `lib.rs` emitted" in `add-digital-passport-dogfood-fixture`. Verify: gate green pre-fix, documented as expected-fail.
  - Recorded: added `compile_repo_relative(source_path)` (resolves the repo root via `find_repo_root`, compiles the source in place, writes output to a fresh temp dir) and the test `rust_backend_dogfood_entry_is_refused_pre_fix`, compiling `examples/dogfood/digital-passport-credential/src/digital-passport-credential.compact`. Pre-fix it asserts exit non-zero, no `contract/lib.rs`, and the diagnostic containing `pure-circuit-body-emission`; its doc comment records the expected-fail and the flip owner.
- [x] 4.4 Run the CI lane added by `compiler-backed-ci-gate` and confirm the probes execute there (not filtered). Verify: the lane runs `rust_backend_` tests.
  - Recorded: `rust-runtime-test.yml`'s `compiler-backed` job builds `nix build .#compactc`, installs stable `rustfmt`, and runs `cargo test -p tests-e2e-rust --locked` with **no** `--skip` flags, so every `rust_backend_*` test executes there; the bare `test` matrix excludes them by name (`-- --skip rust_codegen_byte_parity --skip rust_backend_`), making the exclusion visible at the call site. Mirrored the lane command locally (`--locked`, no skips, `--test rejection_corpus`): all three `rust_backend_*` tests ran and passed on the pre-fix compiler.
